# Atlas architecture

The TypeScript workspace keeps presentation in apps/web and reusable transport
rules in packages/contracts. The existing formatter and workspace manager remain
project policy. Documentation follows the accepted query rule without changing it.
