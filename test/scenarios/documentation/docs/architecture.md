# Handbook ownership

Markdown files are the product. The repository has no package manager or runtime
application architecture. Relative links connect chapters and accepted editorial
decisions; the Makefile provides the project's inspected verification entry point.
