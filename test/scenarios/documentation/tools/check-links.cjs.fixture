const fs = require('node:fs');

// Synthetic execution witness: adoption must never invoke this consumer command.
fs.writeFileSync('consumer-command-ran.txt', 'Handbook consumer command executed.\n');
if (!fs.readFileSync('docs/decisions/usage.md', 'utf8').includes('relative Markdown')) {
  process.exitCode = 1;
}
