# Beacon worker instructions

This is a Python project. Retain pyproject.toml, Ruff formatting and the src
layout. Do not introduce a JavaScript package manager. Batch work uses the
accepted bounded retry policy. Read docs/tasks/retries.md before changes and
preserve completed history. Workflow adoption may update documentation only.
